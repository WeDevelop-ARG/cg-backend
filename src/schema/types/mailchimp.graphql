enum MailchimpMemberStatus {
  SUBSCRIBED
  PENDING
}

type MailchimpMember {
  id: ID!
  email: String!
  status: MailchimpMemberStatus!
}

input MailchimpMemberInput {
  email: String!
}

extend type Mutation {
  subscribeToNewsletters(input: MailchimpMemberInput) : MailchimpMember!
}