type Product {
  id: ID!
  photoUrl: String!
  name: String!
  description: String
  price: Float!
  marketPrice: Float!
}

input ProductInput {
  photoUrl: String!
  name: String!
  description: String
  price: Float!
  marketPrice: Float!
}

extend type Query {
  products: [Product!]!
  product(id: ID!) : Product
}

extend type Mutation {
  createProduct(input: ProductInput!): Product!
}