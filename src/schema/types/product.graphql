type Product {
  id: ID!
  photoUrl: String!
  name: String!
  description: String
  price: Float!
  marketPrice: Float!
}

extend type Query {
  getProducts: [Product!]!
  getProductById(id: ID!) : Product!
}