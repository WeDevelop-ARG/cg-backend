type S3Payload {
  signedRequest: String!
  url: String!
}

input SignedFileUploadURLInput {
  filename: String!
  filetype: String!
}

extend type Mutation {
  createSignedFileUploadURL(input: SignedFileUploadURLInput!): S3Payload!
}