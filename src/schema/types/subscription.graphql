type Subscription {
  id: ID!
  userId: ID!
  groupId: ID!
}

input GroupSubscriptionInput {
  userId: ID!
  groupId: ID!
}

extend type Mutation {
  subscribeToGroup(input: GroupSubscriptionInput!): Subscription!
}