type Group {
  id: ID!
  product: Product!
  type: GroupTypeEnum!
  minParticipants: Int!
  maxParticipants: Int
  discount: Int!
  expiresAt: Date!
}

enum GroupTypeEnum {
  PAIR
  GROUP
}

input GroupInput {
  id: ID!
  productId: ID!
  type: GroupTypeEnum!
  minParticipants: Int!
  maxParticipants: Int
  discount: Int!
  expiresAt: Date!
}

extend type Query {
  groups: [Group!]!
}

# Mutations
extend type Mutation {
  createGroup(input: GroupInput!): Group!
}