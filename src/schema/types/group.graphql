type Group {
  id: ID!
  product: Product!
  type: TypeEnum!
  minParticipants: Int!
  maxParticipants: Int
  discount: Int!
  expiresAt: Date!
}

enum TypeEnum {
  PAIR
  GROUP
}

extend type Query {
  groups: [Group!]!
}